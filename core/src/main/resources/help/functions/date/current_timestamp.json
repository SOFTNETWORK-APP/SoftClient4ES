{
  "name": "CURRENT_TIMESTAMP",
  "category": "Date/Time",
  "shortDescription": "Get current date and time",
  "syntax": [
    "CURRENT_TIMESTAMP",
    "CURRENT_TIMESTAMP()"
  ],
  "description": "Returns the current date and time with timezone (UTC).",
  "parameters": [],
  "returnType": "TIMESTAMP",
  "examples": [
    {
      "title": "Get current time",
      "description": "Use in query",
      "sql": "SELECT CURRENT_TIMESTAMP as now_ts FROM logs"
    },
    {
      "title": "Filter on the current time",
      "description": "Rows created before now",
      "sql": "SELECT message FROM logs WHERE created_at < CURRENT_TIMESTAMP"
    }
  ],
  "notes": [
    "Returns timestamp in UTC",
    "Includes milliseconds",
    "A function call is not accepted in INSERT ... VALUES; only literals are"
  ],
  "seeAlso": ["CURRENT_DATE", "NOW", "TODAY"],
  "aliases": []
}
