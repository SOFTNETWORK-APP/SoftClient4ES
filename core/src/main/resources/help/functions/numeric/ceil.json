{
  "name": "CEIL",
  "category": "Numeric",
  "shortDescription": "Round up to nearest integer",
  "syntax": [
    "CEIL(number)",
    "CEILING(number)"
  ],
  "description": "Rounds a number up to the nearest integer.",
  "parameters": [
    {
      "name": "number",
      "type": "NUMERIC",
      "description": "Number to round up",
      "optional": false,
      "defaultValue": null
    }
  ],
  "returnType": "BIGINT",
  "examples": [
    {
      "title": "Round up",
      "description": "Round a stored average up",
      "sql": "SELECT CEIL(average_rating) as rounded_up FROM listings"
    }
  ],
  "notes": [
    "Arithmetic is not accepted inside a scalar function argument: compute the value into a column, or apply the arithmetic outside the call"
  ],
  "seeAlso": ["FLOOR", "ROUND"],
  "aliases": ["CEILING"]
}
