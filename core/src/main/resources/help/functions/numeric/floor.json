{
  "name": "FLOOR",
  "category": "Numeric",
  "shortDescription": "Round down to nearest integer",
  "syntax": [
    "FLOOR(number)"
  ],
  "description": "Rounds a number down to the nearest integer.",
  "parameters": [
    {
      "name": "number",
      "type": "NUMERIC",
      "description": "Number to round down",
      "optional": false,
      "defaultValue": null
    }
  ],
  "returnType": "BIGINT",
  "examples": [
    {
      "title": "Round down",
      "description": "Drop the fractional part of a balance",
      "sql": "SELECT FLOOR(balance) as whole_units FROM accounts"
    }
  ],
  "notes": [
    "Arithmetic is not accepted inside a scalar function argument: compute the value into a column, or apply the arithmetic outside the call"
  ],
  "seeAlso": ["CEIL", "ROUND"],
  "aliases": []
}
